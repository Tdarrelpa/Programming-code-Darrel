package main

import (
	"fmt"
	"math"
)

func main() {
	var x,y,z float64;
	fmt.Print("Nilai : ");
	fmt.Scan(&x,&y,&z);
	max := math.Max(x,y);
	max = math.Max(max, z)
	min := math.Min(x,y);
	min = math.Min(min, z)
 	hasil := max + min
	fmt.Println( hasil/2 == x || hasil/2== y || hasil/2 == z);
}
