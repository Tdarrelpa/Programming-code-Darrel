package main

import "fmt"

func main () { 
	var x float32

	fmt.Print("Tolong masukan angka untuk nilai x apa pun :")
	fmt.Scan(&x)

	var fx = (( x * x * x ) + (3 * x )) / (( x * x * x * x ) - ( 3 * x * x ) + 4)
	fmt.Println(fx) // hasil sudah dibulatkan
}
