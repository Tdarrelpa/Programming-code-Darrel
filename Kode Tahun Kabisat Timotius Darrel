package main

import "fmt"

func main() {
    var tahun int
    var Kabisat bool
    
    fmt.Print("Input tahun: ")
    fmt.Scan(&tahun)
    
    Kabisat = tahun % 400 == 0 || tahun % 4 == 0 && tahun % 100 != 0
    fmt.Println("Tahun tersebut merupakan tahun kabisat:", Kabisat)
}
